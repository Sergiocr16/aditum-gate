# Dependencias
node_modules/

# Entorno Python del dispositivo (lo crea bootstrap.sh / self-update.sh)
.venv/

# Cache de Angular (el build dist/ SI se versiona: las Pis no compilan)
web/pedestal-app/.angular/

# Identidad y estado local del dispositivo (provisionados por Pi, nunca al repo)
device-id.txt
device-token.txt
config-runtime.json
hikvision-cards.json

# Credenciales del login admin del editor local (hash + secreto de sesion)
admin-credentials.json
admin-session-secret

# Python
__pycache__/
*.pyc

# IDE / SO
.idea/
.vscode/
.DS_Store
